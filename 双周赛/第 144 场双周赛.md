# 3360. 移除石头游戏

```js
/**
 * @param {number} n
 * @return {boolean}
 */
var canAliceWin = function (n) {
  if (n < 10) {
    return false;
  }
  let k = 10;
  while (n >= k) {
    n -= k;
    k--;
  }
  return k % 2 !== 0;
};
```